@model IEnumerable<Lastadmissionproject.Models.MeritList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicantDetail.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HigherSecondaryAggregateMarks)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rank)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicantDetail.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HigherSecondaryAggregateMarks)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rank)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MeritId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MeritId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MeritId })
        </td>
    </tr>
}

</table>
