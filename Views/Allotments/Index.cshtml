@model IEnumerable<Lastadmissionproject.Models.Allotment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ApplicantDetail.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Courses.CourseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ApplicantDetail.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Courses.CourseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllocationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllocationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllocationId })
        </td>
    </tr>
}

</table>
