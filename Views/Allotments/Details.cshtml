@model Lastadmissionproject.Models.Allotment

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Allotment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ApplicantDetail.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ApplicantDetail.FullName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Courses.CourseName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Courses.CourseName)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.AllocationId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
